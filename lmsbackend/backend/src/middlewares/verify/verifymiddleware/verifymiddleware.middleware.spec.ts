import { VerifymiddlewareMiddleware } from './verifymiddleware.middleware';

describe('VerifymiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new VerifymiddlewareMiddleware()).toBeDefined();
  });
});
